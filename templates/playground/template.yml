parameters:
  - name:     preItems
    type:     object
    default:  []
  - name:     items
    type:     object
    default:  []

steps:
  - ${{ each i in parameters.preItems }}:
    - pwsh: 'Write-Host ${{ i }}'
  - ${{ each i in parameters.items }}:
    - pwsh: 'Write-Host ${{ i }}'
