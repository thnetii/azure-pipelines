parameters: {}
steps:
- task: NuGetCommand@2
  displayName: Install MSBuild Logger
  inputs:
    command: custom
    arguments: install -NonInteractive -OutputDirectory bld -ExcludeVersion -Prerelease -Source https://pkgs.dev.azure.com/thnetii/artifacts/_packaging/public/nuget/v3/index.json THNETII.AzureDevOps.Pipelines.MSBuild
